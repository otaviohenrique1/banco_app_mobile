import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  scrollView: {
    marginTop: 10,
  },
  icone: {
    padding: 20,
    backgroundColor: "gray",
    borderRadius: 100,
    width: 65,
  },
});